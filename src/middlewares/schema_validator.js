const asyncHandler = require('../utils/programming/asyncHandler');
const customError = require('../utils/error/customError');

const schema_validator = asyncHandler(async (req, res, next) => {

});

module.exports = schema_validator;